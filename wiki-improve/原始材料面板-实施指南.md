# 原始材料面板 — 待办清单

> 给 Sharon：你的原型（`原始材料面板 - 优化原型.html`）就是这个功能的蓝图和最终验收标准。后端你已经在 `8d0ac970` 提前做完了，而且质量完全不像第一次动这个模块的人写的——回填策略（groupId 留给下次扫描自然补写，不做一次性匹配脚本）是全提交最成熟的工程判断；单列 `groupId` + "永不覆盖手动改挂"的粘性规则，比上游 issue [#487](https://github.com/mateaix/mateclaw/issues/487) 原本的两列设计更简洁地解决了同一个冲突，还用 `incrementalScan_skipHitAlreadyGroupedNotRetagged` 把规则锁进了测试；IDOR 六端点全覆盖、MySQL 幂等迁移引 V146/V161 先例、partial-PUT 遵循 #93/#477 惯例——这些都说明你把仓库的历史读进去了。
>
> 剩下的事按下面三步走。这份清单可以直接作为你和 AI 助手协作的施工上下文——工程判断、方案取舍和最终验收都在你手里（`8d0ac970` 里那几个漂亮的设计决策就是证明），AI 负责把你的决定落成代码。卡在迁移方言、upstream 同步或 issue 流程，直接找倪程伟。

---

## 1. PR-A：前端增强（从 `4705c077` 拆出，先合这个）

分支基于 `dev`，只留纯前端部分：

**保留**：搜索/筛选/排序/状态 chips、多选 + 批量操作栏（tri-state 分组勾选、filter 变化自动裁剪选中集这两个细节写得很讲究，原样保留）、在线预览、异常文件分区、紧凑上传区。分组外壳保留单一"未分组文档"组即可。

**移出（留给 PR-C）**：路径配置弹窗、"添加扫描路径分组"卡片、分组头齿轮和排期徽章、全部 localStorage 读写、`wiki.pathConfig.*` i18n（PR-C 原样加回，文案写得挺好别删）。

**同时恢复**：`dev` 上原有的目录输入框 + 扫描按钮行——过渡期唯一的手动扫描入口，PR-C 再用你原型的"每路径独立扫描"替换。

**顺手修四个点**：

1. 预览加大小上限：`fileSize` 超 2MB 显示"文件过大，请下载查看"，否则大 CSV 卡死页面。
2. `batchReprocess` 循环里只调 API，结束后统一刷新一次（现在选 20 个会触发 60 次列表刷新）。
3. 文案：筛选栏"路径"→"来源类型"；"批量重扫"→"批量重新处理"（它不触发扫描）。
4. 计数口径：状态 chip"失败"只计 `failed`，与异常分区对齐。

**验收**：build/lint 过；功能手工过一遍；目录扫描入口可用；中英 i18n 键对齐；PR 描述注明"路径分组配置见 issue #487，独立 PR 接入"。

---

## 2. PR-B 收尾：`8d0ac970` 评审待办

**合并门槛（2 项）：**

1. **迁移版本号撞车**：V166/V167 与 upstream dev 已有的 V166 冲突，下次同步 Flyway 直接启动失败。先合入 upstream/dev，再把两个迁移文件重命名到空闲版本号（三套方言同步改）。upstream 同步的 merge 可以由倪程伟来做，你只管重命名。
2. **`WikiSourceGroupEntity.deleted` 加 `@TableLogic`**：对齐 wiki 模块的逻辑删除约定（现在 `deleteById` 是物理删除）。

**同 PR 小改（4 项）：**

3. 删掉 `scan_mode` 列（扫描端点只用 `?mode=` 参数，这列没人读；顺带消灭将来"定时全量烧 token"的隐患）。
4. `cronExpr` 创建/更新时用 `CronExpression.parse()` 校验，坏表达式当场 400。将来接调度器时：cron 触发只允许增量。
5. `(kb_id, alias)` 加唯一索引 + create 查重（注意 `@TableLogic` 之后唯一索引要处理软删行，方言写法找倪程伟对）。
6. `updateRawGroupBatch` 归属过滤改成 `selectList(in(id, rawIds).eq(kbId))` 一次查询，并补一个测试：批量请求混入外库 rawId，断言实际 update 的 id 集合不含它（断言落在绑定参数上）。

**最值得补的一个测试**：你在 `updateGroup` javadoc 里自己指出的坑——NOT_NULL 策略下必须用 `LambdaUpdateWrapper.set` 才能把 groupId 置空——目前没有测试验证（服务层测试 mock 掉了 mapper）。补一个 H2 落库测试：设组 → 清空为 null → 断言真的变 null。

**可讨论（不阻塞，PR 描述里提一句即可）**：raw 列表向 viewer 返回 `sourcePath`（服务器绝对路径）是否有意；全量扫描会重跑已被迁到其它组的文件（不改归属，语义上写明或过滤 `groupId == group.id`）；`WikiRawMaterialEntity.sourcePath` 的 "binary uploads only" 注释已过时，顺手更正。

---

## 3. PR-C：接线，让原型完整落地

把 PR-A 移出的部分加回来接真实 API（端点前缀 `/api/v1/wiki/knowledge-bases/{kbId}`）：

| 现状 | 改为 |
|---|---|
| `pathConfigs` / `customGroupKeys`（localStorage） | `GET /source-groups`（VO 自带 `rawCount`/`lastScanAt`，分组统计和"最后扫描"直接用）；删光 localStorage 和 `frontendOnlyHint` |
| `confirmAddPath()` | `POST /source-groups` |
| `saveConfig()` / `removePath()` | `PUT` / `DELETE /source-groups/{groupId}`（删除可带 `?reassignTo=` 改挂目标组） |
| `groupedMaterials` 全落 `UNGROUPED_KEY` | 按 raw 的 `groupId` 分组；NULL → "未分组文档"；`GROUP_ORDER`/rank 逻辑删掉 |
| 排期徽章读 localStorage | 读 `cronExpr` + `enabled`；调度器接入前徽章加"未生效"角标，别让用户以为定时已在跑 |
| （缺失）分组头"单路径增量扫描"⟳ | `POST /source-groups/{groupId}/scan?mode=incremental`（全量 `mode=full`） |
| （缺失）迁移分组 | 原型 `moveGroupModal` + 批量栏按钮，调 `PATCH /raw/group`（单条 `PATCH /raw/{rawId}/group`，`groupId: null` = 未分组） |
| PR-A 恢复的旧扫描行 | 删除，同时清理 `wiki.scan/scanning/scanResult/dirPlaceholder` 等孤儿 i18n 键 |
| 预览弹窗（加分项） | 加"查看生成的页面 →"，调现成的 `store.filterPagesByRaw` |

**验收**：对照 `原始材料面板 - 优化原型.html` 逐区块走查——路径配置、分组折叠、单路径扫描、定时开关、迁移分组、异常分区、预览。原型即验收标准。
